server:
  port: 9002

spring:
  application:
    name: "authentication-service"

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true

solutions:
  matusek:
    jwt:
      issuer: "solutions.matusek"
      algorithm: "RSA"
      keySize: 2048
      expirationSeconds: 3600
